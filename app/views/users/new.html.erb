<h1>Sign Up</h1>
<p>ここからチャットルームに投稿するためのアカウントを作成できるよ！</p>

<p>＊ここに入力フォームを作成</p>



<section>
	<div class="container">
		<div class="col-md">
			<%= form_for(@user) do |f| %>
				<%= f.label :name %>
				<%= f.text.field :name %>
				<%= f.label :name %>
				<%= f.text.field :email %>
			<% end %>
		</div>
	</div>
</section>